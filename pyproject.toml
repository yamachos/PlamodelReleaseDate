[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PlamodelReleaseDate"
version = "0.1.0"
description = "PlamodelReleaseDate - minimal Python project"
readme = "README.md"
requires-python = ">=3.8"
authors = [ { name = "Hiroshi" } ]
dependencies = []

[tool.pytest.ini_options]
minversion = "6.0"
