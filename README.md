# PlamodelReleaseDate

Minimal Python project scaffold generated by assistant.

## Quickstart

Install dev requirements and run tests:

```powershell
python -m pip install -U pip
python -m pip install -r requirements.txt
pytest
```
